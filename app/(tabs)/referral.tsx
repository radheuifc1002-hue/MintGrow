import React, { useState, useEffect } from 'react';
import { View, Text, StyleSheet, ScrollView, Share, TextInput, Pressable, Alert, KeyboardAvoidingView, Platform } from 'react-native';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { MaterialIcons } from '@expo/vector-icons';
import { useGame } from '@/hooks/useGame';
import { GlowButton } from '@/components/ui/GlowButton';
import { Colors, Spacing, Radius, Typography } from '@/constants/theme';
import { REFERRAL_BONUS_TOKENS, TOKEN_NETWORK, WITHDRAWAL_MIN, REFERRAL_LEVELS } from '@/types/game';
import { getReferralsCached, applyReferralCodeInDatabase } from '@/services/database';
import { ReferralEntry } from '@/types/game';

export default function ReferralScreen() {
  const insets = useSafeAreaInsets();
  const { profile, refreshProfile } = useGame();
  const [referrals, setReferrals] = useState<ReferralEntry[]>([]);
  const [codeInput, setCodeInput] = useState('');
  const [applying, setApplying] = useState(false);
  const [showLevels, setShowLevels] = useState(false);
  const [copied, setCopied] = useState(false);

  useEffect(() => { loadReferrals(); }, [profile?.telegramId]);
  const loadReferrals = async (forceRefresh = false) => { if (!profile?.telegramId) return; setReferrals(await getReferralsCached(profile.telegramId, forceRefresh)); };

  const handleCopy = async () => {
    const code = profile?.referralCode?.trim(); if (!code) return;
    try {
      if (Platform.OS === 'web' && typeof navigator !== 'undefined' && navigator.clipboard?.writeText) {
        await navigator.clipboard.writeText(code);
      } else if (Platform.OS === 'web' && typeof document !== 'undefined') {
        const input = document.createElement('textarea'); input.value = code; input.style.position = 'fixed'; input.style.opacity = '0'; document.body.appendChild(input); input.focus(); input.select(); document.execCommand('copy'); document.body.removeChild(input);
      } else {
        await Share.share({ message: code, title: 'MintGrow Referral Code' });
      }
      setCopied(true); setTimeout(() => setCopied(false), 1800);
    } catch { Alert.alert('Copy failed', 'Please select and copy the referral code manually.'); }
  };

  const handleShare = async () => {
    if (!profile?.referralCode) return;
    try { await Share.share({ message: `Join MintGrow — merge crypto coins and EARN real MG tokens on BNB Chain!\n\nUse my referral code: ${profile.referralCode}\n\nGet 100 MG welcome bonus + earn together!\n\n#MintGrow #BNBChain #CryptoGame`, title: 'Join MintGrow Game' }); } catch {}
  };

  const handleApplyCode = async () => {
    if (!codeInput.trim()) return Alert.alert('Enter Code', 'Please enter a referral code');
    if (profile?.referredBy) return Alert.alert('Already Referred', 'You have already applied a referral code');
    setApplying(true);
    try {
      const result = await applyReferralCodeInDatabase(profile!.telegramId, codeInput.trim());
      if (result.ok) { await refreshProfile(); await loadReferrals(true); setCodeInput(''); Alert.alert('Welcome Bonus', 'Referral code applied. You received 100 MG bonus tokens.'); }
      else { const messages: Record<string, string> = { invalid_code: 'This referral code does not exist.', already_referred: 'You have already used a referral code.', self_referral: 'You cannot use your own referral code.', referee_not_found: 'Your player account was not found.' }; Alert.alert('Referral Code', messages[result.reason || ''] || 'The referral code could not be applied.'); }
    } finally { setApplying(false); }
  };

  const directRefs = profile?.referralCount ?? 0;
  const totalReferralEarnings = profile?.referralTokensEarned ?? 0;
  const eligibleLevels = REFERRAL_LEVELS.filter(l => directRefs >= l.directRequired);
  const howItems = [
    { icon: 'share', text: 'Share your referral code with friends' },
    { icon: 'card-giftcard', text: `Friend uses code → gets 100 MG welcome bonus + you get ${REFERRAL_BONUS_TOKENS} MG` },
    { icon: 'trending-up', text: 'You earn a percentage of their tokens when they withdraw, subject to your direct-referral requirements' },
    { icon: 'lock-open', text: 'L1 requires 2 direct refs. L2 requires 2. L3 requires 3. Higher levels follow the table.' },
    { icon: 'account-balance-wallet', text: `Affiliate income is released when your referral reaches ${WITHDRAWAL_MIN.toLocaleString()} MG and withdraws` },
  ];

  return <KeyboardAvoidingView style={{ flex: 1 }} behavior={Platform.OS === 'ios' ? 'padding' : 'height'}><View style={[styles.container, { paddingTop: insets.top }]}><ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={styles.scroll}>
    <View style={styles.titleRow}><View style={styles.titleIcon}><MaterialIcons name="group-add" size={22} color={Colors.primary} /></View><View><Text style={styles.pageTitle}>Referral Program</Text><Text style={styles.pageSubtitle}>Invite friends · Earn MG tokens at 25 levels</Text></View></View>
    <View style={styles.statsRow}><View style={[styles.statCard, styles.statCardGreen]}><MaterialIcons name="person-add" size={20} color={Colors.primary} /><Text style={[styles.statVal, { color: Colors.primary }]}>{directRefs}</Text><Text style={styles.statLbl}>Direct Refs</Text></View><View style={styles.statCard}><MaterialIcons name="payments" size={20} color={Colors.primary} /><Text style={[styles.statVal, { color: Colors.primary }]}>{totalReferralEarnings.toFixed(0)} MG</Text><Text style={styles.statLbl}>Ref Earnings</Text></View><View style={styles.statCard}><MaterialIcons name="military-tech" size={20} color={Colors.primary} /><Text style={[styles.statVal, { color: Colors.primary }]}>{eligibleLevels.length}/25</Text><Text style={styles.statLbl}>Active Levels</Text></View></View>
    <View style={styles.ruleNote}><MaterialIcons name="info-outline" size={14} color={Colors.info} /><Text style={styles.ruleText}>Referral income is credited when your referral reaches the {WITHDRAWAL_MIN.toLocaleString()} MG withdrawal threshold and calls withdraw.</Text></View>
    <View style={styles.levelsCard}><Pressable style={styles.levelsHeader} onPress={() => setShowLevels(v => !v)}><Text style={styles.levelsTitle}>25-Level Income Structure</Text><MaterialIcons name={showLevels ? 'expand-less' : 'expand-more'} size={22} color={Colors.primary} /></Pressable>{showLevels && <View style={styles.levelsBody}><View style={styles.levelHeaderRow}><Text style={[styles.levelCell, { flex: .6 }]}>Level</Text><Text style={[styles.levelCell, { flex: .8 }]}>Income</Text><Text style={[styles.levelCell, { flex: 1.2 }]}>Need Direct</Text><Text style={[styles.levelCell, { flex: .8, textAlign: 'right' }]}>Status</Text></View>{REFERRAL_LEVELS.map(rl => { const eligible = directRefs >= rl.directRequired; return <View key={rl.level} style={[styles.levelRow, eligible && styles.eligibleRow]}><Text style={[styles.levelCell, styles.levelCellNum, { flex: .6 }]}>L{rl.level}</Text><Text style={[styles.levelCell, { flex: .8, color: Colors.primary, fontWeight: '700' }]}>{(rl.pct * 100).toFixed(0)}%</Text><Text style={[styles.levelCell, { flex: 1.2 }]}>{rl.directRequired} refs</Text><View style={{ flex: .8, alignItems: 'flex-end' }}>{eligible ? <View style={styles.activePill}><MaterialIcons name="check" size={14} color="#fff" /></View> : <View style={styles.lockPill}><MaterialIcons name="lock-outline" size={13} color={Colors.textMuted} /><Text style={styles.lockText}>{rl.directRequired - directRefs} more</Text></View>}</View></View>; })}</View>}</View>
    <View style={styles.codeSection}><Text style={styles.sectionTitle}>Your Referral Code</Text><View style={styles.codeBox}><Text style={styles.codeText}>{profile?.referralCode || '...'}</Text><Pressable style={styles.copyBtn} onPress={handleCopy} accessibilityRole="button" accessibilityLabel="Copy referral code"><MaterialIcons name={copied ? 'check' : 'content-copy'} size={18} color={Colors.primary} /></Pressable></View>{copied && <Text style={styles.copiedText}>Referral code copied</Text>}<GlowButton label="Share Code & Invite Friends" onPress={handleShare} variant="primary" fullWidth style={{ marginTop: Spacing.sm }} /></View>
    {!profile?.referredBy ? <View style={styles.applySection}><Text style={styles.sectionTitle}>Have a Referral Code?</Text><Text style={styles.sectionSub}>Enter a friend's code to get 100 MG bonus</Text><View style={styles.inputRow}><TextInput style={styles.input} value={codeInput} onChangeText={v => setCodeInput(v.toUpperCase())} placeholder="e.g. MG1234XYZ" placeholderTextColor={Colors.textMuted} autoCapitalize="characters" autoCorrect={false} maxLength={20} /><GlowButton label={applying ? '...' : 'Apply'} onPress={handleApplyCode} disabled={applying || !codeInput.trim()} variant="primary" style={{ paddingHorizontal: Spacing.md }} /></View></View> : <View style={styles.referredBadge}><MaterialIcons name="check-circle" size={18} color={Colors.success} /><Text style={styles.referredText}>Referred by code: {profile.referredBy}</Text></View>}
    <View style={styles.howCard}><Text style={styles.howTitle}>How Earnings Work</Text>{howItems.map((item, i) => <View key={i} style={styles.howItem}><View style={styles.howIcon}><MaterialIcons name={item.icon as any} size={17} color={Colors.primary} /></View><Text style={styles.howText}>{item.text}</Text></View>)}</View>
    <View style={styles.listSection}><View style={styles.listHeader}><Text style={styles.sectionTitle}>Referral Activity ({referrals.length})</Text><Pressable style={styles.refreshBtn} onPress={() => loadReferrals(true)}><MaterialIcons name="refresh" size={16} color={Colors.primary} /></Pressable></View>{referrals.length === 0 ? <View style={styles.empty}><MaterialIcons name="group" size={36} color={Colors.textMuted} /><Text style={styles.emptyText}>No referrals yet. Share your code.</Text></View> : referrals.map((r, i) => { const hasReached = (r.refereeBalance ?? 0) >= WITHDRAWAL_MIN; return <View key={i} style={styles.referralItem}><View style={styles.referralAvatar}><Text style={styles.referralAvatarText}>{r.username[0]?.toUpperCase()}</Text></View><View style={styles.referralInfo}><Text style={styles.referralName}>@{r.username}</Text><Text style={styles.referralDate}>{new Date(r.joinedAt).toLocaleDateString()}</Text>{hasReached && <Text style={styles.referralReached}>Reached withdrawal threshold ✓</Text>}</View><View style={styles.referralEarning}><Text style={styles.referralEarningAmt}>+{r.tokensEarned.toFixed(0)}</Text><Text style={styles.referralEarningUnit}>MG</Text></View></View>; })}</View>
    <View style={styles.networkNote}><MaterialIcons name="info-outline" size={14} color={Colors.primary} /><Text style={styles.networkText}>MG tokens distributed on {TOKEN_NETWORK}</Text></View>
  </ScrollView></View></KeyboardAvoidingView>;
}

const styles = StyleSheet.create({ container: { flex: 1, backgroundColor: Colors.bg }, scroll: { padding: Spacing.md, paddingBottom: 40 }, titleRow: { flexDirection: 'row', alignItems: 'center', gap: 10, marginBottom: Spacing.md }, titleIcon: { width: 44, height: 44, borderRadius: 14, backgroundColor: Colors.primaryGlow, alignItems: 'center', justifyContent: 'center' }, pageTitle: { ...Typography.h2, color: Colors.textPrimary, marginBottom: 2 }, pageSubtitle: { ...Typography.small, color: Colors.textMuted }, statsRow: { flexDirection: 'row', gap: Spacing.sm, marginBottom: Spacing.sm }, statCard: { flex: 1, backgroundColor: Colors.bgCard, borderRadius: Radius.lg, padding: Spacing.sm, alignItems: 'center', borderWidth: 1, borderColor: Colors.border, gap: 4 }, statCardGreen: { borderColor: Colors.primary }, statVal: { fontSize: 18, fontWeight: '800' }, statLbl: { ...Typography.caption, color: Colors.textMuted }, ruleNote: { flexDirection: 'row', alignItems: 'flex-start', gap: 6, backgroundColor: '#E3F2FD', borderRadius: Radius.sm, padding: Spacing.sm, borderWidth: 1, borderColor: '#0277BD', marginBottom: Spacing.md }, ruleText: { ...Typography.small, color: '#0277BD', flex: 1, lineHeight: 18 }, levelsCard: { backgroundColor: Colors.bgCard, borderRadius: Radius.lg, borderWidth: 1, borderColor: Colors.border, marginBottom: Spacing.md, overflow: 'hidden' }, levelsHeader: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', padding: Spacing.md }, levelsTitle: { ...Typography.bodyBold, color: Colors.textPrimary }, levelsBody: { paddingHorizontal: Spacing.md, paddingBottom: Spacing.md }, levelHeaderRow: { flexDirection: 'row', paddingVertical: 4, borderBottomWidth: 1, borderBottomColor: Colors.border, marginBottom: 4 }, levelRow: { flexDirection: 'row', paddingVertical: 6, borderBottomWidth: 1, borderBottomColor: Colors.bgSurface, alignItems: 'center' }, eligibleRow: { backgroundColor: Colors.primaryGlow, borderRadius: 4, paddingHorizontal: 4 }, levelCell: { ...Typography.small, color: Colors.textSecondary }, levelCellNum: { color: Colors.textPrimary, fontWeight: '700' }, activePill: { backgroundColor: Colors.primary, borderRadius: Radius.full, paddingVertical: 3, paddingHorizontal: 8 }, lockPill: { flexDirection: 'row', alignItems: 'center', gap: 3 }, lockText: { ...Typography.caption, color: Colors.textMuted }, codeSection: { backgroundColor: Colors.bgCard, borderRadius: Radius.lg, padding: Spacing.md, marginBottom: Spacing.md, borderWidth: 1, borderColor: Colors.primary }, sectionTitle: { ...Typography.bodyBold, color: Colors.textPrimary, marginBottom: 4 }, sectionSub: { ...Typography.small, color: Colors.textMuted, marginBottom: Spacing.md }, codeBox: { flexDirection: 'row', alignItems: 'center', backgroundColor: Colors.bgSurface, borderRadius: Radius.md, padding: Spacing.md, borderWidth: 1, borderColor: Colors.borderStrong, gap: Spacing.sm }, codeText: { flex: 1, fontSize: 20, fontWeight: '800', color: Colors.primary, letterSpacing: 4 }, copyBtn: { padding: 6 }, copiedText: { ...Typography.caption, color: Colors.success, marginTop: 5 }, applySection: { backgroundColor: Colors.bgCard, borderRadius: Radius.lg, padding: Spacing.md, marginBottom: Spacing.md, borderWidth: 1, borderColor: Colors.border }, inputRow: { flexDirection: 'row', gap: Spacing.sm, alignItems: 'center' }, input: { flex: 1, backgroundColor: Colors.bgSurface, borderRadius: Radius.md, borderWidth: 1, borderColor: Colors.border, paddingHorizontal: Spacing.md, paddingVertical: 12, color: Colors.textPrimary, fontSize: 15, fontWeight: '700', letterSpacing: 2, minHeight: 48 }, referredBadge: { flexDirection: 'row', alignItems: 'center', gap: 6, backgroundColor: Colors.primaryGlow, borderRadius: Radius.md, padding: Spacing.md, marginBottom: Spacing.md, borderWidth: 1, borderColor: Colors.primary }, referredText: { ...Typography.small, color: Colors.primary }, howCard: { backgroundColor: Colors.bgCard, borderRadius: Radius.lg, padding: Spacing.md, marginBottom: Spacing.md, borderWidth: 1, borderColor: Colors.border, gap: Spacing.sm }, howTitle: { ...Typography.bodyBold, color: Colors.textPrimary, marginBottom: 4 }, howItem: { flexDirection: 'row', alignItems: 'flex-start', gap: Spacing.sm }, howIcon: { width: 28, height: 28, borderRadius: 14, backgroundColor: Colors.primaryGlow, alignItems: 'center', justifyContent: 'center' }, howText: { ...Typography.small, color: Colors.textSecondary, flex: 1, lineHeight: 20 }, listSection: { backgroundColor: Colors.bgCard, borderRadius: Radius.lg, padding: Spacing.md, marginBottom: Spacing.md, borderWidth: 1, borderColor: Colors.border }, listHeader: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: Spacing.sm }, refreshBtn: { width: 30, height: 30, borderRadius: 15, backgroundColor: Colors.bgSurface, alignItems: 'center', justifyContent: 'center', borderWidth: 1, borderColor: Colors.border }, empty: { alignItems: 'center', paddingVertical: Spacing.lg, gap: 8 }, emptyText: { ...Typography.body, color: Colors.textMuted }, referralItem: { flexDirection: 'row', alignItems: 'center', backgroundColor: Colors.bgSurface, borderRadius: Radius.md, padding: Spacing.sm, marginBottom: Spacing.sm, borderWidth: 1, borderColor: Colors.border, gap: Spacing.sm }, referralAvatar: { width: 36, height: 36, borderRadius: 18, backgroundColor: Colors.primaryGlow, borderWidth: 1, borderColor: Colors.primary, alignItems: 'center', justifyContent: 'center' }, referralAvatarText: { ...Typography.bodyBold, color: Colors.primary }, referralInfo: { flex: 1, gap: 2 }, referralName: { ...Typography.smallBold, color: Colors.textPrimary }, referralDate: { ...Typography.caption, color: Colors.textMuted }, referralReached: { ...Typography.caption, color: Colors.success, fontWeight: '600' }, referralEarning: { alignItems: 'flex-end' }, referralEarningAmt: { ...Typography.bodyBold, color: Colors.primary }, referralEarningUnit: { ...Typography.caption, color: Colors.textMuted }, networkNote: { flexDirection: 'row', alignItems: 'center', gap: 6, backgroundColor: Colors.primaryGlow, borderRadius: Radius.sm, padding: Spacing.sm }, networkText: { ...Typography.small, color: Colors.primary, flex: 1 } });
