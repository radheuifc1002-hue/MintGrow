import {useState,useCallback} from'react';import{getWithdrawals,getProfile,saveWithdrawal,saveProfile,getStakeRequests}from'@/services/storage';import{WithdrawalRequest,WITHDRAWAL_MIN,TOKEN_NETWORK}from'@/types/game';
const BEP20_ADDRESS_PATTERN=/^0x[a-fA-F0-9]{40}$/;export const normalizeWalletAddress=(address:string)=>address.trim();export const isValidBep20Address=(address:string)=>BEP20_ADDRESS_PATTERN.test(normalizeWalletAddress(address));
export function useWithdrawal(){const[withdrawals,setWithdrawals]=useState<WithdrawalRequest[]>([]),[isLoading,setIsLoading]=useState(false),[error,setError]=useState<string|null>(null);const loadWithdrawals=useCallback(async()=>{const p=await getProfile();if(!p)return;setWithdrawals(await getWithdrawals(p.telegramId))},[]);const requestWithdrawal=useCallback(async(amount:number):Promise<boolean> =>{setError(null);const p=await getProfile();if(!p){setError('Profile not found');return false}if(!p.walletAddress){setError('Please set your BEP-20 wallet address first');return false}if(p.totalTokens<WITHDRAWAL_MIN||amount<WITHDRAWAL_MIN){setError(`Minimum withdrawal is ${WITHDRAWAL_MIN.toLocaleString()} MG`);return false}if(amount>p.totalTokens){setError('Insufficient token balance');return false}const stakes=await getStakeRequests();if(!stakes.some(s=>s.status==='confirmed')){setError('A successful on-chain stake is required before withdrawal');return false}setIsLoading(true);try{const req:WithdrawalRequest={id:`wd_${Date.now()}_${Math.random().toString(36).slice(2,8)}`,telegramId:p.telegramId,username:p.username,amount,walletAddress:p.walletAddress,network:TOKEN_NETWORK,status:'pending',createdAt:new Date().toISOString()};await saveWithdrawal(req);await loadWithdrawals();return true}catch(e){setError(e instanceof Error?e.message:'Failed to submit. Please try again.');return false}finally{setIsLoading(false)}},[loadWithdrawals]);const updateWallet=useCallback(async(address:string):Promise<boolean>=>{const p=await getProfile();if(!p){setError('Profile not found');return false}const normalized=normalizeWalletAddress(address);if(!isValidBep20Address(normalized)){setError('Please enter a valid BEP-20 wallet address');return false}setIsLoading(true);setError(null);try{p.walletAddress=normalized;await saveProfile(p);return true}catch{setError('Failed to save wallet address. Please try again.');return false}finally{setIsLoading(false)}},[]);return{withdrawals,isLoading,error,loadWithdrawals,requestWithdrawal,updateWallet,setError}}